[package]
name = "dpor-prototype"
version = "0.1.0"
edition = "2021"
description = "Prototype DPOR (Dynamic Partial Order Reduction) engine for interlace"

[lib]
name = "dpor_prototype"
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
