[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "frontrun-dpor"
version = "0.1.0"
description = "DPOR engine for frontrun concurrency testing"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
